interface:
  display_name: "JJ Stack Organizer"
  short_description: "Organize jj stacks with split and label plans."
  default_prompt: "Use $jj-stack-organizer to analyze @ plus unlabeled ancestors and return one fenced bash block with any needed jj split -m and jj describe -r ... -m ... commands."
