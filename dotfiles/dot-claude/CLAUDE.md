Whenever you are within a directory with format "**/.work/*", then assume that this is a jujutsu workspace.
You should use the jj subagent instead of git to work with version control.
Stay within the workspace that matches "**/.work/*" and do not try to make changes outside of it.
