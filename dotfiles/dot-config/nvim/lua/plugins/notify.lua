return {
  "rcarriga/nvim-notify",
  -- event = "VeryLazy", -- noice will load this
  opts = {
    render = "wrapped-compact",
    fps = 144,
    timeout = 200,
    max_height = 5,
    max_width = 50,
  },
}
