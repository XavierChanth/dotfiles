# TODO

- [ ] Replace lazy.nvim with native package manager
  - [ ] Build my own loading system using `plugin/` and `after/plugin/`
- [ ] Move keybinds to a single file instead of mixed in config `after/plugin/`
