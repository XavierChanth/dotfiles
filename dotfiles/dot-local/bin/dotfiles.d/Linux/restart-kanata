#!/usr/bin/env bash

install_systemd_service() {
  dir=$1
  file=$2
  sudo cp "$dir/$file" "/etc/systemd/system/"
  sudo systemctl enable $file
  sudo systemctl restart $file
}

if [ "$(uname)" = 'Linux' ]; then
  install_systemd_service "$DOTFILES/dotfiles/dot-config/kanata" "kanata.service"
fi
