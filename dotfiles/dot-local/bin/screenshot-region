#!/usr/bin/env bash
# Capture screenshot of selected region with annotation

grim -g "$(slurp)" - | satty --filename - --fullscreen --output-filename ~/Pictures/Screenshots/screenshot-$(date +%Y%m%d-%H%M%S).png
